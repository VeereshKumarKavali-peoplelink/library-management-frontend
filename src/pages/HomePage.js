import React from 'react';
import BookList from '../components/BookList';

const HomePage = () => {
    return (
        <div>
            <BookList />
        </div>
    );
};

export default HomePage;
